services:

  

  web:
   image: nginx:latest
   volumes:
     - ./:/var/www
     - ./.docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
   ports:
     - "8081:80"
   environment:
     - NGINX_HOST=localhost
